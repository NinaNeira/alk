<div class="user">
    <div class="user-header">
        <div class="row h-100 m-0">
            <div class="col-12 align-self-center">
                <h3>Twoje zamówienia</h3>
            </div>
        </div>
    </div>
    <div class="user-content">
        <ul class="list-group">
            {{#each data}}
            <li id="{{_id}}" class="list-group-item">
                {{trim _id}}
                {{#each order.items}}
                {{item.name}}
                {{/each}}

                {{order.totalPrice}}
                <i class="fas fa-plane-slash" data-toggle="modal" data-target="#order-cancel" data-order="{{_id}}">
                </i></li>
            {{/each}}
        </ul>
        <div class="modal fade" id="order-cancel" tabindex="-1" role="dialog" aria-labelledby="order-cancel-title"
            aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="order-cancel-title">Odwołanie rezerwacji</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Zamknij">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        Czy na pewno chcesz odwołać rezerwację?
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Anuluj</button>
                        <button type="button" class="btn btn-primary confirm-cancel"
                            data-id="{{_id}}">Potwierdzam</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>